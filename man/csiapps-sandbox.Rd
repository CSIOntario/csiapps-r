% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sandbox.R
\name{csiapps-sandbox}
\alias{csiapps-sandbox}
\title{Sandbox mode for local development}
\description{
Sandbox mode lets developers run the full schema -> validate -> ingest ->
retrieve warehouse workflow entirely locally, with no network access and no
authentication. \strong{Sandbox mode is enabled by default}, so that development
work never reaches the production warehouse by accident: the same
\code{\link[=make_request]{make_request()}} calls are routed to a local, in-memory warehouse instead of
the REST API. It is disabled for deployment with
\code{options(csiapps.sandbox = FALSE)} (or by setting \code{CSIAPPS_ENV=production}),
and can be overridden per call with the \code{sandbox} argument of
\code{\link[=make_request]{make_request()}}. Because the production and sandbox workflows use the same
calls, no code changes are needed when moving from development to deployment.

The sandbox emulates three endpoints:
\itemize{
\item \verb{GET api/warehouse/data-sources/\{uuid\}} -- returns the schema previously
registered with \code{\link[=register_sandbox_schema]{register_sandbox_schema()}}, nested under
\code{head_primary_definition$schema} exactly like the real API.
\item \verb{POST api/warehouse/ingestion/primary/} -- validates \code{body$records}
against the registered schema for \code{body$source} (using \code{jsonvalidate})
and, on success, stores them in memory and writes the JSON payload to a
per-source folder on disk for inspection (see \code{\link[=browse_sandbox]{browse_sandbox()}}).
\item \verb{GET api/warehouse/data-records} -- returns previously ingested records
for \code{query$source_uuid}, each wrapped in the same envelope
(\code{data}, \code{subject}, \code{created_at}, ...) the real API returns.
}

Any other endpoint raises an error. Sandbox state lasts for the R session;
use \code{\link[=clear_sandbox]{clear_sandbox()}} to reset it between tests.
}
\section{Shiny app wrappers}{

Sandbox mode also lets a wrapped Shiny app (see \code{\link[=ui_wrapper]{ui_wrapper()}},
\code{\link[=server_wrapper]{server_wrapper()}}, \code{\link[=check_secrets]{check_secrets()}}) run locally without the OAuth2
redirect. The redirect exists only to obtain an access token, and requires
client credentials that cannot be safely distributed, so in sandbox mode
\code{\link[=server_wrapper]{server_wrapper()}} \strong{simulates the login} instead: it seeds the session from
the developer's existing \code{CSIAPPS_ACCESS_TOKEN} and hands it to the same code
path a production login would. If that token is present, \verb{/me} and the
organization list are loaded from the \strong{real} registration API, so the
developer sees their real identity and organizations. If no token is set, the
app shell still renders but shows an unauthenticated notice prompting the
developer to set a read-only \code{CSIAPPS_ACCESS_TOKEN}. The same wrapped-app code
therefore runs in both modes; only the \code{csiapps.sandbox} option differs.
}

\section{Limitations}{

\itemize{
\item \strong{Sandbox is not fully offline for wrapped apps.} Warehouse endpoints
routed through \code{\link[=make_request]{make_request()}} are emulated locally, but the wrapper's
registration reads (\verb{/me}, organizations, profiles) bypass \code{\link[=make_request]{make_request()}}
and call the real API with your token. Sandbox mode is thus deliberately
split: warehouse data is emulated, registration/auth data is real. Set the
institute with \code{\link[=set_institute]{set_institute()}} to match the institute that issued your
token, or those reads will be rejected.
The sandbox faithfully simulates the \emph{schema contract}, not the warehouse.
Anything that depends on server-side state will differ from production:
\item \strong{Validation parity is approximate.} Sandbox ingestion validates records
against the JSON Schema with Ajv (via \code{jsonvalidate}), which catches the
most common failure modes (missing required fields, wrong types, length
and pattern violations). The real server additionally enforces things the
schema cannot express: resolution of \code{subject_field} values against
registered profiles, duplicate/dataset handling against existing data,
and token permissions. Validator implementations may also differ on edge
cases (e.g. \code{format} keyword enforcement). Passing sandbox validation is
therefore a \emph{necessary but not sufficient} condition for production
acceptance -- do not treat a green sandbox run as a guarantee.
\item \strong{\code{subject} is always \code{NULL} in retrieved records.} In production the
server links each ingested record to a registered profile and returns it
(name, sport, ...) in the record envelope. The sandbox has no profile
registry, so it cannot emulate this linkage and returns \code{subject = NULL}
rather than fabricating misleading data. Code that displays or filters on
subject fields will see placeholder values, and mistyped \code{subject_field}
values that production would flag are accepted silently.
}
}

