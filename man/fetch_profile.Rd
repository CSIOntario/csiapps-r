% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{fetch_profile}
\alias{fetch_profile}
\title{Fetch a single profile from the CSIAPPS registration API}
\usage{
fetch_profile(token = NULL, profile_id, sandbox = is_sandbox_mode())
}
\arguments{
\item{token}{Character. Authentication token. When not supplied, it is
resolved for the current Shiny session (the token stored by
\code{\link[=server_wrapper]{server_wrapper()}}) and otherwise falls back to the \code{CSIAPPS_ACCESS_TOKEN}
environment variable.}

\item{profile_id}{Integer or character. The ID of the profile to retrieve.}

\item{sandbox}{Logical. When \code{TRUE} (the default in development), no network
call is made and the profile is looked up in the local dummy registry (those
created with \code{\link[=create_profile]{create_profile()}}), returning \code{NULL} if no such id exists. Set
to \code{FALSE} to fetch the real profile from the API. Defaults to
\code{\link[=is_sandbox_mode]{is_sandbox_mode()}}.}
}
\value{
A single profile object as a list, or \code{NULL} if no profile with that
id exists. The structure mirrors the list elements returned by
\code{\link[=fetch_profiles]{fetch_profiles()}}.
}
\description{
Retrieves one profile by its ID.
}
\examples{
\dontrun{
set_institute("csiontario")
profile <- fetch_profile(profile_id = 123L, sandbox = FALSE)
profile$person$first_name
}
}
\seealso{
\code{\link[=fetch_profiles]{fetch_profiles()}} to retrieve multiple profiles,
\code{\link[=set_institute]{set_institute()}} to configure the target institute.
}
