% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quarto.R
\name{quarto_setup}
\alias{quarto_setup}
\title{Add CSI authentication and snapshot data to an interactive Quarto document}
\usage{
quarto_setup(
  snapshot_data = function() list(),
  protected_body = ".csiapps-protected-body.html",
  config_file = ".csiapps-quarto.json",
  sandbox = is_sandbox_mode()
)
}
\arguments{
\item{snapshot_data}{A zero-argument function returning a named list of
datasets. Each dataset must be a data frame or a list of named records.
The function executes once, after authentication, in the viewer's Shiny
session. Defaults to an empty registry.}

\item{protected_body}{Path to the private body file generated by the Quarto
extension. The default is correct for a standalone document.}

\item{config_file}{Path to the private runtime configuration generated by the
extension. When present, its institute is applied before OAuth starts.}

\item{sandbox}{Whether to use the existing csiapps sandbox authentication
behavior. Defaults to \code{\link[=is_sandbox_mode]{is_sandbox_mode()}}.}
}
\value{
Invisibly returns \code{NULL}. The function registers Shiny observers for
the active session.
}
\description{
Call this once from a server-executed cell in a Quarto HTML document or
dashboard that uses \code{server: shiny} and the \code{csiapps} Quarto extension. It
reuses \code{\link[=server_wrapper]{server_wrapper()}} for CSI OAuth and sends the protected report body
and registered datasets to the browser only after the session is signed in.
}
