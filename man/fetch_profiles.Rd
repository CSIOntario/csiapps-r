% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{fetch_profiles}
\alias{fetch_profiles}
\title{Fetch profiles from the CSIAPPS registration API}
\usage{
fetch_profiles(token = NULL, filters = list(), sandbox = is_sandbox_mode())
}
\arguments{
\item{token}{Character. Authentication token. Defaults to the
\code{CSIAPPS_ACCESS_TOKEN} environment variable.}

\item{filters}{Named list of query parameters for filtering. Common filters
include \code{sport_org_id} (integer, filter by organisation) and \code{sport}
(filter by sport). See the
\href{https://apps.csiontario.ca/api/swagger/}{CSIAPPS Swagger docs} for all
available parameters.}

\item{sandbox}{Logical. When \code{TRUE} (the default in development), no network
call is made and an empty list is returned immediately. Set to \code{FALSE} for
production to fetch real profiles. Defaults to \code{\link[=is_sandbox_mode]{is_sandbox_mode()}}.}
}
\value{
A list of profile objects. Each element contains a \code{person} sub-list
(\code{first_name}, \code{last_name}, \code{dob}, \code{email}, ...) and a
\code{current_nomination} sub-list (\code{role}, \code{organization}, ...). See the
\href{https://apps.csiontario.ca/api/swagger/}{CSIAPPS Swagger docs} for the
full schema.
}
\description{
Retrieves all profiles accessible to the authenticated user, with optional
filtering. Automatically paginates — all matching profiles are returned in a
single list regardless of how many pages the API uses.
}
\examples{
\dontrun{
set_institute("csiontario")

# All profiles your token can see
profiles <- fetch_profiles(sandbox = FALSE)

# Profiles for a specific organisation
profiles <- fetch_profiles(filters = list(sport_org_id = 42L), sandbox = FALSE)

# Build a display data frame
profile_df <- do.call(rbind, lapply(profiles, function(p) {
  data.frame(
    id         = p$id,
    first_name = p$person$first_name \%||\% NA_character_,
    last_name  = p$person$last_name  \%||\% NA_character_,
    email      = p$person$email      \%||\% NA_character_
  )
}))
}
}
\seealso{
\code{\link[=fetch_profile]{fetch_profile()}} to retrieve a single profile by ID,
\code{\link[=fetch_org_options]{fetch_org_options()}} to list available organisations,
\code{\link[=set_institute]{set_institute()}} to configure the target institute.
}
