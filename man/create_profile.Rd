% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sandbox.R
\name{create_profile}
\alias{create_profile}
\title{Create dummy athlete profiles in the sandbox}
\usage{
create_profile(n, sport_org_id)
}
\arguments{
\item{n}{Integer. Number of profiles to create.}

\item{sport_org_id}{Integer. Id of the sport org (from \code{\link[=create_sport_org]{create_sport_org()}})
the athletes belong to. Stored as each profile's \code{sport$id}, which is what
\code{fetch_profiles(filters = list(sport_org_id = ...))} filters on.}
}
\value{
The created profiles, invisibly. Appended to any already registered.
}
\description{
Generates \code{n} random athlete profiles and registers them under an existing
sandbox sport org, so sandbox \code{fetch_profiles()} / \code{fetch_profile()} return
prod-shaped data. The org must already exist (see \code{\link[=create_sport_org]{create_sport_org()}}).
}
\examples{
org <- create_sport_org()
create_profile(3, org$id)
clear_sandbox()
}
\seealso{
\code{\link[=create_sport_org]{create_sport_org()}}, \link{csiapps-sandbox}
}
