% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{fetch_org_options}
\alias{fetch_org_options}
\title{Fetch organisation options from the CSIAPPS registration API}
\usage{
fetch_org_options(token = NULL, sandbox = is_sandbox_mode())
}
\arguments{
\item{token}{Character. Authentication token. When not supplied, it is
resolved for the current Shiny session (the token stored by
\code{\link[=server_wrapper]{server_wrapper()}}) and otherwise falls back to the \code{CSIAPPS_ACCESS_TOKEN}
environment variable.}

\item{sandbox}{Logical. When \code{TRUE} (the default in development), no network
call is made and the local dummy registry is returned (the orgs registered
with \code{\link[=create_sport_org]{create_sport_org()}}). Set to \code{FALSE} to fetch real organisations from
the API. Defaults to \code{\link[=is_sandbox_mode]{is_sandbox_mode()}}.}
}
\value{
A list of named lists, each with \code{label} (organisation name) and
\code{value} (organisation ID).
}
\description{
Returns all organisations accessible to the authenticated user as a list of
\code{label}/\code{value} pairs, suitable for use in Shiny \code{selectInput()} choices.
}
\examples{
\dontrun{
set_institute("csiontario")
orgs <- fetch_org_options(sandbox = FALSE)
selectInput("org", "Organisation", choices = orgs)
}
}
\seealso{
\code{\link[=fetch_profiles]{fetch_profiles()}} to fetch profiles, \code{\link[=set_institute]{set_institute()}} to
configure the target institute.
}
