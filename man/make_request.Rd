% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{make_request}
\alias{make_request}
\title{Make an authenticated API request to CSIAPPS}
\usage{
make_request(
  endpoint,
  method = "GET",
  body = NULL,
  query = list(),
  headers = list(),
  token = NULL,
  timeout = 20L,
  verbose = FALSE,
  paginate = FALSE,
  max_pages = 50,
  sandbox = is_sandbox_mode()
)
}
\arguments{
\item{endpoint}{API endpoint path.}

\item{method}{HTTP method. Defaults to "GET"}

\item{body}{Optional request body for POST/PUT/PATCH requests; should be an R object that can be serialized to JSON}

\item{query}{Optional list of query parameters to include in the request URL}

\item{headers}{Optional list of additional HTTP headers to include in the request}

\item{token}{Authentication token. When not provided, it is resolved for the current Shiny session (the token stored by \code{\link[=server_wrapper]{server_wrapper()}}), falling back to the \code{CSIAPPS_ACCESS_TOKEN} environment variable outside a session. Inside a Shiny session a not-yet-available token does not error: the request is cancelled quietly with \code{\link[shiny:req]{shiny::req()}}, and the calling reactive or observer re-runs automatically once login completes (see \code{\link[=token_ready]{token_ready()}}). Outside Shiny a missing token raises an error.}

\item{timeout}{Request timeout in seconds; defaults to 20}

\item{verbose}{If TRUE, prints request and response details to the console for debugging purposes}

\item{paginate}{If TRUE, will attempt to paginate through results using "next" links in the API response. Defaults to FALSE.}

\item{max_pages}{Maximum number of pages to fetch when paginate = TRUE; defaults to 50 to prevent infinite loops}

\item{sandbox}{If TRUE, the request is routed to the local sandbox instead of
the real REST API: no network call is made and no authentication is required.
Only the \strong{warehouse} endpoints are emulated; registration and auth endpoints
(e.g. \code{api/registration/...}, \verb{api/csiauth/me/}) are not, and raise a 501 in
sandbox unless \code{sandbox = FALSE} (for registration reads, use
\code{\link[=fetch_org_options]{fetch_org_options()}} / \code{\link[=fetch_profiles]{fetch_profiles()}} instead). Defaults to
\code{\link[=is_sandbox_mode]{is_sandbox_mode()}}, which is \strong{TRUE by default}. Disable sandbox mode
globally with \code{options(csiapps.sandbox = FALSE)} (or \code{CSIAPPS_ENV=production})
to route requests to the production warehouse. See \link{csiapps-sandbox} for
supported endpoints and limitations. In sandbox mode the HTTP-only arguments
(\code{headers}, \code{token}, \code{timeout}, \code{max_pages}) are ignored, since no network
request is made.}
}
\value{
List of parsed API responses
}
\description{
Make an authenticated API request to CSIAPPS
}
